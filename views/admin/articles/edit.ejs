<div class="max-w-5xl mx-auto p-6">

    <h1 class="text-3xl font-bold mb-8 text-slate-100">
        แก้ไขบทความ
    </h1>

    <form
        action="/admin/articles/edit/<%= article.id %>"
        method="POST"
        enctype="multipart/form-data"
        class="space-y-6 text-slate-400"
    >

        <div>

            <label class="font-medium">
                ชื่อบทความ
            </label>

            <input
                type="text"
                name="title"
                value="<%= article.title %>"
                class="w-full border rounded p-2 bg-slate-900 border-slate-700 text-slate-100 mt-2"
            >

        </div>
        <div>

            <label class="font-medium">
                slug
            </label>

            <input
                type="text"
                name="slug"
                value="<%= article.slug %>"
                class="w-full border rounded p-2 bg-slate-900 border-slate-700 text-slate-100 mt-2"
            >

        </div>

        <div>

            <label>
                คำอธิบายสั้น
            </label>

            <textarea
                name="short_description"
                rows="4"
                class="w-full border rounded p-2 bg-slate-900 border-slate-700 text-slate-100 mt-2"
            ><%= article.short_description %></textarea>

        </div>

        <div>

            <label>
                เนื้อหา
            </label>

            <textarea
                name="content"
                rows="15"
                class="w-full border rounded p-2 bg-slate-900 border-slate-700 text-slate-100 mt-2"
            ><%= article.content %></textarea>

        </div>

        <div>
            <label class="block font-medium mb-2">รูปปก</label>

            <% if (article.featured_image) { %>
                <img
                    src="/images/articles/<%= article.featured_image %>"
                    class="w-48 rounded mb-3">
            <% } %>

            <label
                for="featured_image"
                class="inline-flex items-center gap-2 px-4 py-2 bg-orange-500 hover:bg-orange-600 text-white rounded cursor-pointer">
                เลือกรูปภาพ
            </label>

            <span id="file-name" class="ml-3 text-slate-400">
                ยังไม่ได้เลือกไฟล์
            </span>

            <input
                id="featured_image"
                type="file"
                name="featured_image"
                accept="image/*"
                class="hidden">

            <input
                type="hidden"
                name="old_featured_image"
                value="<%= article.featured_image %>">
        </div>
        <div>

            <label>
                Meta Title
            </label>

            <input
                type="text"
                name="meta_title"
                value="<%= article.meta_title %>"
                class="w-full border rounded p-2 bg-slate-900 border-slate-700 text-slate-100 mt-2"
            >

        </div>

        <div>

            <label>
                Meta Description
            </label>

            <textarea
                name="meta_description"
                rows="3"
                class="w-full border rounded p-2 bg-slate-900 border-slate-700 text-slate-100 mt-2"
            ><%= article.meta_description %></textarea>

        </div>
        <div>

            <label>
                OG Title
            </label>

            <input
                type="text"
                name="og_title"
                value="<%= article.og_title %>"
                class="w-full border rounded p-2 bg-slate-900 border-slate-700 text-slate-100 mt-2"
            >

        </div>
        <div>

            <label>
                OG Description
            </label>

            <textarea
                name="og_description"
                rows="3"
                class="w-full border rounded p-2 bg-slate-900 border-slate-700 text-slate-100 mt-2"
            ><%= article.og_description %></textarea>

        </div>
        <div>

            <label>
                Canonical Url
            </label>

            <input
                type="text"
                name="canonical_url"
                value="<%= article.canonical_url %>"
                class="w-full border rounded p-2 bg-slate-900 border-slate-700 text-slate-100 mt-2"
            >

        </div>
        <div>

            <label>
                Keywords
            </label>

            <input
                type="text"
                name="keywords"
                value="<%= article.keywords %>"
                class="w-full border rounded p-2 bg-slate-900 border-slate-700 text-slate-100 mt-2"
            >

        </div>
        <div>
            <label class="block font-medium">
                บทความแนะนำ
            </label>

            <select
                name="is_featured"
                class="w-full border rounded p-2 bg-slate-900 border-slate-700 text-slate-100 mt-2"
            >
                <option value="0" <%= article.is_featured == 0 ? 'selected' : '' %>>
                    ไม่แนะนำ
                </option>

                <option value="1" <%= article.is_featured == 1 ? 'selected' : '' %>>
                    แนะนำ
                </option>
            </select>
        </div>
        <div>
            <label class="block font-medium">
                สถานะโพสต์
            </label>

            <select
                name="status"
                class="w-full border rounded p-2 bg-slate-900 border-slate-700 text-slate-100 mt-2"
            >
                <option value="draft" <%= article.status === 'draft' ? 'selected' : '' %>>
                    Draft
                </option>

                <option value="published" <%= article.status === 'published' ? 'selected' : '' %>>
                    Published
                </option>
            </select>
        </div>

        <button
            class="bg-orange-600 text-slate-100 px-6 py-3 rounded hover:bg-orange-700"
        >
            บันทึกการแก้ไข
        </button>

    </form>

</div>